PORT=3000
JWT_SECRET=change_this_to_long_random_secret_minimum_32_chars

# Firebase Realtime Database URL
FIREBASE_DATABASE_URL=https://cashty-cd354-default-rtdb.firebaseio.com

# Firebase service account JSON (for GitHub Actions / production)
# Paste the full JSON string as one line — no newlines inside the value
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"cashty-cd354",...}
